| ID | Filename | Problem manifestation | Root cause (math/logic) | Correction implemented | Commit hash |
| --- | --- | --- | --- | --- | --- |
| BUGFIX_1 | data.py | Validation set reused part of the training set (data leakage), causing validation metrics to reflect training data instead of unseen data | train_data and train_labels were assigned the full tensor with no slicing, so val_data (sliced from [val_start:]) was a strict subset of train_data | Sliced train_data/train_labels to [:val_start] and kept val_data/val_labels at [val_start:], ensuring fully disjoint train and validation splits | [583268c](https://github.com/Abin-28/MAI-IDL2026_Final-Project-Assignment_Priyam-Jha_And_Abin-Skaria/commit/583268c46b166e94c9432f68e70a13a63567fae9) |
| BUGFIX_2 | data.py | get_loaders() immediately raises FileNotFoundError on every call because the constructed file path does not match the actual dataset filenames on disk | Path was built as f"{data}_data.pt" but restored files are stored as f"{data}.pt" — the _data suffix points to a non-existent file | Changed path construction to f"{data}.pt" so the loader correctly resolves to the actual recovered dataset files | [0ef8202](https://github.com/Abin-28/MAI-IDL2026_Final-Project-Assignment_Priyam-Jha_And_Abin-Skaria/commit/0ef8202db8831a3f43a24647d81e876810ec692f) |
| BUGFIX_3 | data.py | Image pixel values were passed directly into the models in their raw [0, 255] range without any normalisation, causing poor and unstable training across all models and datasets | The data loader never rescaled or standardised the image tensors — models received inputs far outside the numeric range they are designed to work with, leading to degraded accuracy | Divided all image tensors by 255.0 to bring values into [0, 1], then subtracted the mean and divided by the std computed from the training set only; std is clamped to 1e-8 to prevent division by zero on constant channels; same mean/std applied to val and test sets | [5d1554c](https://github.com/Abin-28/MAI-IDL2026_Final-Project-Assignment_Priyam-Jha_And_Abin-Skaria/commit/5d1554c1a2b779000af791be09ac5cf1d3ef0d2d) |